interface:
  display_name: "Debug Nova"
  short_description: "Debug Nova failures with LLDB and repo breakpoints"
  default_prompt: "Use $debug-nova to debug this Nova failure with repo-specific workflows."
